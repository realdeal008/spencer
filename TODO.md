# TODO

- [x] Fix broken `next/image` src paths in `app/component/about.jsx` (remove `/public/` prefix for files served from `public/`).
- [x] Add missing `sizes` prop for all `next/image` components using `fill` in `app/component/about.jsx`.



